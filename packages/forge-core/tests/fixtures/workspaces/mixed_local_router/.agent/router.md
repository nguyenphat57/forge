## Scope Policy

Global orchestrator is `forge-antigravity`.

## Local Skill Inventory

- `react-vite-typescript`
- `capacitor-android`
- `offline-first-dexie-sync`

## Routing Map

Use `react-vite-typescript` for React/Vite UI work.
Use `capacitor-android` for Android shell work.
Use `offline-first-dexie-sync` for offline sync flows.
