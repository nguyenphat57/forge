---
name: test
type: compatibility-wrapper
canonical_skill: forge-test-driven-development
---

# Test Compatibility Wrapper

Use host-native skill discovery to invoke `forge-test-driven-development`.

This file exists for `/test` and legacy workflow paths only. It is not the testing source of truth.

Invoke `forge-test-driven-development` for feature, bugfix, regression, refactor, or behavior-changing work with a viable harness.

If there is no viable harness, record the no-harness reason and strongest fallback proof, then verify before claims with `forge-verification-before-completion`.
