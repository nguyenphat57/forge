---
name: debug
type: compatibility-wrapper
canonical_skill: forge-systematic-debugging
---

# Debug Compatibility Wrapper

Use host-native skill discovery to invoke `forge-systematic-debugging`.

This file exists for `/debug` and legacy workflow paths only. It is not the debugging source of truth.

Invoke `forge-systematic-debugging` before fixing bugs, regressions, failing tests, prompt bugs, or unexpected behavior.

Fix only after root cause or a reproducible hypothesis is established.
