---
name: rollback
type: compatibility-wrapper
---

# Rollback Compatibility Wrapper

Use this file only as a thin compatibility wrapper.

Canonical activation stays natural-language first, `python scripts/repo_operator.py rollback --scope <scope> --format json`, and host-native `forge-*` skill discovery.

Plan rollback through `python scripts/resolve_rollback.py --scope <scope>`.

State scope, data-loss risk, and post-rollback verification before recommending action.
