---
name: run
type: compatibility-wrapper
---

# Run Compatibility Wrapper

Use this file only as a thin compatibility wrapper.

Canonical activation stays natural-language first, `python scripts/repo_operator.py run --workspace <workspace> --timeout-ms 20000 -- <command>`, and host-native `forge-*` skill discovery.

Run the command through `python commands/run_with_guidance.py --workspace <workspace> --timeout-ms 20000 -- <command>`.

Report the actual output or failure signal and route the next step from evidence.

