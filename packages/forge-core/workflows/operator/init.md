---
name: init
type: compatibility-wrapper
---

# Init Compatibility Wrapper

Use this file only as a thin compatibility wrapper.

Canonical activation stays natural-language first, `python scripts/repo_operator.py init --workspace <workspace> --format json`, and host-native `forge-*` skill discovery.

Preview or apply through `python scripts/initialize_workspace.py --workspace <workspace> [--seed-preferences] [--apply]`.

Do not overwrite existing files, and keep onboarding minimal and host-neutral.
