---
name: next
type: compatibility-wrapper
---

# Next Compatibility Wrapper

Use this file only as a thin compatibility wrapper.

Canonical activation stays natural-language first, `python scripts/repo_operator.py next --workspace <workspace> --format json`, and host-native `forge-*` skill discovery.

Resolve the recommendation through `python commands/resolve_help_next.py --workspace <workspace> --mode next`.

Return one concrete next step anchored to current repo state without expanding scope.

