---
name: architect
type: compatibility-wrapper
canonical_skill: forge-brainstorming
---

# Architect Compatibility Wrapper

Use host-native skill discovery to invoke `forge-brainstorming`.

Architecture is now a design lens inside brainstorming, not a separate canonical process source.

Use this wrapper only when a legacy alias requests architecture, system boundaries, data ownership, compatibility, migration, auth, payment, or public API shape.

For the current canonical guidance, read `references/design/architectural-lens.md` from the core bundle after invoking `forge-brainstorming`.

If the architectural question changes direction, stay in `forge-brainstorming`; if approved, hand off to `forge-writing-plans`.
