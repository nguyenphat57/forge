# Stack Profile

Companion target:
- Next.js App Router
- TypeScript-first codebase
- Postgres-oriented data layer, with Prisma as the default baseline

Primary assumptions:
- web application is the product surface
- server-side data access lives behind typed modules
- schema and env hygiene are part of the verification bar
